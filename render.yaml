services:
- type: web
  name: stock-data-api
  env: python
  pythonVersion: 3.9  # <--- Ensure this line is added/updated
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn app:app
  plan: free
